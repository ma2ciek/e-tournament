/// <reference path="../typings/index.d.ts" />
import * as assert from 'assert';

describe('App', () => {
    describe('simpleTest', () => {
        it('app should return hello world', () => {
            assert.ok(1);
        });
    });
});
